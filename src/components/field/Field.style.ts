import styled from "styled-components";

const Field = styled.div `
    width: 430px;
    height: 430px;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    transform-style: preserve-3d;
    perspective: 500px;
`;

export const Style = {
    Field
}